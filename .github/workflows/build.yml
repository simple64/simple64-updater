name: Build project

on:
  push:
    branches:
      - main
  pull_request:

env:
  PYINSTALLER_VERSION: "5.5"
  PYTHON_VERSION: "3.10"

jobs:
  build-linux:
    runs-on: ubuntu-latest
    container: registry.access.redhat.com/ubi9/ubi:latest
    steps:
      - uses: actions/checkout@v3
      - name: Update and install packages
        run: |
          dnf -y update
          dnf -y install python3-pip python3-tkinter binutils
          python3 -m pip install --upgrade pip
          python3 -m pip install --upgrade certifi types-requests black flake8 mypy bandit wheel pyinstaller==${{ env.PYINSTALLER_VERSION }}
      - name: Run build script
        run: |
          black --check simple64-updater.py
          flake8 --extend-ignore=E501 simple64-updater.py
          mypy --strict simple64-updater.py
          bandit --skip B404,B603 simple64-updater.py
          python3 -m PyInstaller -F simple64-updater.py
      - name: Upload package
        uses: actions/upload-artifact@v3
        with:
          name: simple64-updater
          path: dist/simple64-updater
  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Run build script
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade certifi requests types-requests black flake8 mypy bandit wheel
          black --check simple64-updater.py
          flake8 --extend-ignore=E501 simple64-updater.py
          mypy --strict simple64-updater.py
          bandit --skip B404,B603 simple64-updater.py
          curl -O -L https://github.com/pyinstaller/pyinstaller/archive/refs/tags/v${{ env.PYINSTALLER_VERSION }}.tar.gz
          tar xf v${{ env.PYINSTALLER_VERSION }}.tar.gz --directory /d
          pushd /d/pyinstaller-${{ env.PYINSTALLER_VERSION }}/bootloader
          python ./waf all
          cd ..
          python setup.py install
          popd
          pyinstaller -w -F simple64-updater.py
      - name: Upload package
        uses: actions/upload-artifact@v3
        with:
          name: simple64-updater.exe
          path: dist/simple64-updater.exe
